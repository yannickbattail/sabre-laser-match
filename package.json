{
  "scripts": {
    "clean": "rm ./src/*.js ./src/*.js.map",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "live-server --wait=1000 ./src"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "live-server": "^1.2.2"
  }
}
